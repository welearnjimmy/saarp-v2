<?php

class PDFExcelUtil {
    
    public static function generatePDF($data, $filename) {
        // Load PDF library (e.g., TCPDF, FPDF)
        // Create a new PDF document
        // Add data to the PDF
        // Output the PDF to the browser or save it to a file
    }

    public static function generateExcel($data, $filename) {
        // Load Excel library (e.g., PhpSpreadsheet)
        // Create a new Excel document
        // Add data to the Excel sheet
        // Output the Excel file to the browser or save it to a file
    }

    public static function downloadFile($filePath) {
        // Set headers for file download
        // Read the file and output it to the browser
    }
}